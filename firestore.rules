rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // El código de acceso de revendedores: cualquiera puede LEERLO
    // (lo necesita el formulario para verificar), pero solo vos
    // (logueado como admin) podés cambiarlo.
    match /settings/resellerAccess {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Sesiones de revendedores: cualquiera puede crear una (al
    // ingresar el código) y actualizar su propia actividad
    // (heartbeat). Solo el admin puede LEER la lista completa
    // o borrar registros.
    match /resellerSessions/{sessionId} {
      allow create: if true;
      allow update: if true;
      allow read: if request.auth != null;
      allow delete: if request.auth != null;
    }
  }
}
